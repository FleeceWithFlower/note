```
var pinyin = require("pinyin");
```

```
regions.forEach((item) => {    let state = {}    let status = obj[pinyin(item.name, {segment: false, style: pinyin.STYLE_FIRST_LETTER,})[0][0]]    if (status) {        obj[pinyin(item.name, {            segment: false,            style: pinyin.STYLE_FIRST_LETTER,        })[0][0]].push(item)    } else {        obj[pinyin(item.name, {segment: false, style: pinyin.STYLE_FIRST_LETTER,})[0][0]] = []        obj[pinyin(item.name, {            segment: false,            style: pinyin.STYLE_FIRST_LETTER,        })[0][0]].push(item)    }})let ary = []for (let key in obj) {    let state = {        key: '',        list: []    }    state.key = key.toUpperCase()    state.list = obj[key]    ary.push(state)}ary.sort(function (a, b) {    let nameA = a.key;    let nameB = b.key;    if (nameA < nameB) {        return -1;    }    if (nameA > nameB) {        return 1;    }    return 0;})
```