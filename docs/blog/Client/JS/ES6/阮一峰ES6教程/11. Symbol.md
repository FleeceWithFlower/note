# Symbol

# 声名：

```
let s = Symbol();
```

# `Symbol`函数可以接受一个字符串作为参数，表示对 Symbol 实例的描述

```
let s1 = Symbol('foo');
```

作为对象的属性名：

 声明：

```
let mySymbol = Symbol();
let a = {
  [mySymbol]: 'Hello!'
};
a[mySymbol] // "Hello!"
```