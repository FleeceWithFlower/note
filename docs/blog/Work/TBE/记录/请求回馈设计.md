#  vux-mutation

1. changeGlobalToast

   - 参数\<object\>

     - toast    = > Toast参数

     - status  = > Toast状态
     - i18n = > 是否文字以 *i18n* 展示

     - postCatch = > 是否文字以 *i18n* 展示

       

# get 

以访问资源为主



- 成功 

  - 反馈：无反馈

  - 方法：



- 失败
  - 反馈：无反馈
  - 方法：

# post

以人机交互为主

- 成功

  - 反馈：有反馈

  - #### 显示

  - 方法：API-POST 调用   vux-mutation-changeGlobalToast 显示 Toast 组件

    - 参数\<object\>

      - toast\<object\>

        - type = >  'success'

        - text = > 'success'

      - status = > true

      - i18n = > true

      - postCatch = > false



- 失败

  - 反馈：

  - 方法：API-POST 调用   vux-mutation-changeGlobalToast 显示 Toast 组件

    - 参数\<object\>

      - toast\<object\>

        - type = >  'warn'

        - text = > status.code

      - status = > true

      - i18n = > true

      - postCatch = > true

